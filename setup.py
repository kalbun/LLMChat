from setuptools import setup, find_packages

setup(
    name='LLMChat',
    version='0.3',
    packages=find_packages(),
    description='A simple python class that allows to easily execute completion tasks on GPT and Claude',
    author='Giuseppe Scarpi',
    author_email='giuseppe.scarpi@unica.it',
    keywords=['LLM', 'GPT'],
)
