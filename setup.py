from setuptools import setup, find_packages

setup(
    name='LLMChat',
    version='0.1',
    packages=find_packages(),
    description='A simple python class that allows to easily execute completion tasks on GPT, Claude and Gemini',
    author='Giuseppe Scarpi',
    author_email='giuseppe.scarpi@unica.it',
    keywords={'LLM', 'GPT'},
)
